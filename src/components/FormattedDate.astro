---
interface Props {
	date: Date;
}

const { date } = Astro.props;
if (!date) {
  throw new Error('FormattedDate: "date" prop is missing');
}
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
